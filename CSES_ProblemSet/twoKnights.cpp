#include <bits/stdc++.h>
using namespace std;
bool attack(int xind, int yind){
    
} 
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    while(n) {
        int ans = 0;
        cout<<ans;
        n--;
    }
    return 0;
}